{
    "name": "web",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "pnpm run build:sxo && pnpm run build:copy",
        "build:sxo": "../bin/sxo.js build && ../bin/sxo.js generate",
        "build:copy": "mkdir -p ./public && cp -r ./dist/client/* ./public/ && cp -r ../components/src/public ./public/",
        "deploy": "wrangler deploy",
        "dev": "wrangler dev",
        "start": "wrangler dev"
    },
    "devDependencies": {
        "wrangler": "^4.45.4"
    }
}
